//http://www.geeksforgeeks.org/two-elements-whose-sum-is-closest-to-zero/
//
//Given an array of elements, find the 2 elements whose sum is closest to zero
//

#include<iostream>
#include<vector>
using namespace std;

int main()
{
    int arr[] ={1, 60, -10, 70, 80, 85};
    vector<int> vec(arr, arr+sizeof(arr)/sizeof(arr[0]));
    

    return 0;
}

